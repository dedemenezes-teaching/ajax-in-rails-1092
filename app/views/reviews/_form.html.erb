<%= simple_form_for [restaurant, review], data: {
                                              action: 'submit->insert-in-list#createReview',
                                              insert_in_list_target: 'form'
                                            } do |f| %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>
